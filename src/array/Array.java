package array;

/*
    - it"s a collection of data elements of specified type.
    - all data holding partitions have contiguous memory location.
    - each partition has two neighbors except first and last one.
    - size of array is fixed and cannot be modified once it s created.
    - being adjacent each partition is indexed and can be determined by its position.
*/

public class Array {
    public static void main(String[] args) {

    }
}
